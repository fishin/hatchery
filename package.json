{
  "name": "hatchery",
  "version": "1.1.0",
  "description": "test functions for ficion",
  "main": "lib/index.js",
  "repository": {
    "type": "git",
    "url": "git://github.com/fishin/hatchery"
  },
  "author": "Lloyd Benson",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/fishin/hatchery/issues"
  },
  "homepage": "https://github.com/fishin/hatchery",
  "engines": {
    "node": ">=4"
  },
  "scripts": {
    "clean": "rm -rf node_modules",
    "console": "lab -a code -t 100 -v -m 2000 -L",
    "test": "lab -a code -t 100 -v -m 2000 -L -r json -o test.lab -r html -o test.html -r console -o stdout -r console -o test.log"
  },
  "dependencies": {
    "hoek": "3.x.x",
    "pail": "git+https://github.com/fishin/pail"
  },
  "devDependencies": {
    "code": "2.x.x",
    "lab": "8.x.x"
  }
}
